============
Installation
============

At the command line::

    $ easy_install boilerplate

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv boilerplate
    $ pip install boilerplate
